<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WordNest</title>
    <link rel="stylesheet" href="/styles/blogPosts.css">
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=PT+Serif:ital,wght@0,400;0,700;1,400;1,700&display=swap"
      rel="stylesheet"
    />
</head>
<body>
    <nav>
      <a href="/"><p class="pt-serif-bold">Word<span>Nest</span></p></a>
      <form action="/post" method="GET">
        <input type="submit" value="Write Blog" class="write-blog">
      </form>
    </nav>
    <div class="post-container">
        <h1 class="main-head pt-serif-bold">Your Blogs</h1>
        <% if (locals.posts && posts.length>0) { %>
            <% for(let i=0;i<posts.length;i++) { %>
                <div class="post">
                    <div class="post-title">
                        <h1 class="pt-serif-bold"> <span>✓</span> <a href="/posts/<%= posts[i].id %>"><%= posts[i].title %></a></h1>
                        <p> <%=posts[i].date %></p>
                    </div>
                    <p class="author">By:<%= posts[i].author %></p>
                    <p class="content">
                        <% if (posts[i].content.length>100) { %>
                           <%- posts[i].content.slice(0,100)%>.....
                        <% }else { %>
                           <%- posts[i].content%>
                        <% } %>
                    </p>
                    <form action="/delete/<%= posts[i].id%>" method="GET">
                        <input type="submit" value="Delete Post" class="delete-btn" onclick="return confirm('Are you sure you want to delete this post?')">
                    </form>
                </div>
            <% } %>
        <% } else { %>
            <h2>No posts found.</h2>
        <% } %>
    </div>
</body>
</html>